PORT=5001

# Frontend URL for CORS (use * to allow all during dev)
FRONTEND_URL=http://localhost:5173

# SMTP credentials – fill these in before running
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=sudhanandhini@sunsys.in
SMTP_PASS=negwpixzdxbljfnf
